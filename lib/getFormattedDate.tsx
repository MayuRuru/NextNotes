// TODO use this function for updating content dinamically in the future

/* export default function getFormattedDate(dateString: string): string {
  return new Intl.DateTimeFormat("es-ES", { dateStyle: "long" }).format(
    new Date(dateString)
  );
}
 */
